{
  "type": "module",
  "scripts": {
    "dev": "npx browser-sync start --server --files '**/*.html,**/*.css,**/*.js,data/*.json' --no-notify --port 8000",
    "test": "vitest run"
  },
  "devDependencies": {
    "jsdom": "^20.0.3",
    "vitest": "^2.1.9"
  }
}
